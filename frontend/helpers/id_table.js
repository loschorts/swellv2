// returns the local id for a spitcast_id
LocalKey = {
	1: 2
};

// returns the spitcast_id for a local id
SpitcastKey = {
	2: 1
};

module.exports = {
	LocalKey: LocalKey,
	SpitcastKey: SpitcastKey
};